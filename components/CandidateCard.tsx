import  { mean,max,min,extent, csv } from 'd3'
import React from 'react'

type Props = {
    name:string
    age:number
    party:string
}

const CandidateCard = async (props: Props) => {
   
  return (
    <div>CandidateCard</div>
  )
}

export default CandidateCard